#ifndef DELSPACE_H_INCLUDED
#define DELSPACE_H_INCLUDED

#include <stdio.h>
#include <stdlib.h>
#include <string.h>

/* функция, удаляющая лишние пробелы из строки
 * входной параметр - данная строка
 * выходной параметр - исправленная строка
 * в качестве возвращаемого значения выступает длина исправленной строки
 */

int *maxint(const char *a, int *maximum);

#endif // DELSPACE_H_INCLUDED
